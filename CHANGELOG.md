# 更新日志

所有版本更新记录。

---

## v1.2.0 (2026-09-11)

### 新增
- 新增 `api_endpoint` 配置项：通过图床接口获取作品页数与原图地址（下拉可选 api.pixiv.cat / api.pixiv.re / api.pixiv.nl 三个镜像域名）
- 新增 `image_proxy` 配置项（替代原 `download_host`）：可选 `i.pixiv.re` / `i.pixiv.nl` / `i.pixiv.cat`（与接口地址独立，可分别切换）
- 新增 `use_proxy` / `proxy_url` 配置项，支持 http/https 与 socks5 代理（默认关闭）
- 新增 `info_cache_ttl` 配置项，缓存作品信息，减少接口调用（接口有速率限制）
- 新增 `max_pages` 配置项，限制单次下载页数上限（默认 200）
- 新增 `reuse_local_files` 配置项（默认开启）：本地保存目录里已存齐整部作品时直接复用发送，不再重复下载；找不到或不全则照常下载（对聊天输出无影响，仅在日志中记录）

### 优化
- 统一下载前提示：单图与多图都会先发一条含「作品ID、作品标题、画师、页数、发送格式」的信息
- 指令指定 `pdf`/`zip` 时，单图也会打包发送（原来单图一律直发图片）
- 图片改为插件自己下载后发送本地文件，代理设置对所有路径生效（不再依赖平台侧联网取图）
- 待发送的图片登记给 AstrBot（`event.track_temporary_local_file`），事件处理结束即自动删除，另保留过期目录兜底清理
- 单页下载遇 HTTP 429/5xx 会自动重试；4xx 立即失败不再无谓重试
- 错误提示区分「图床接口故障」与「作品ID无效」，不再把图床故障误报为作品ID问题
- httpx 显式 `trust_env=False`，避免误用环境变量中的系统代理
- 关闭本地保存时不再创建空的 downloads 目录

### 修复
- 修复图床改版后 `-999.jpg` 页数识别失效导致的“无法识别图床返回的页数信息”报错
- 修复下载域名不可达时提示信息误导的问题
- 修复大图传输中断（如 15MB 原图通过代理时被截断）导致整条指令失败的问题，单页下载失败会自动重试 3 次
- 修复本地保存与打包时扩展名一律写成 `.jpg` 的问题，现按原图真实格式（jpg/png/webp…）命名
- 修复打包格式配置被填成非法值（如 `rar`）时生成同名错误文件的问题，现回退为 `pdf`
- 修复图片地址带查询串时被丢弃的问题

---

## v1.1.0 (2026-07-22)

### 新增
- 新增 `max_free_pack_count` 配置项，多图低于此数量时逐张发送，高于则自动打包
- logger 控制台输出下载进度（单图/多图均支持）
- 新增 `logo.png` 插件图标
- 新增 `CHANGELOG.md` 更新日志
- README 添加市场规范徽章
- 添加 `tags` 字段，便于插件市场发现

### 优化
- 遵循 AstrBot 插件市场规范，统一 desc/version 格式
- 所有配置项添加 hint 说明字段
- 请求头精简，去掉多余的 Referer/Accept
- 保存路径改为 `plugin_data` 安全区，重载插件不丢失
- 多图保存按作品ID归入子目录，不再散放

### 修复
- 修复多图探测逻辑，改用图床 `-999.jpg` 响应判断页数，无需 Ajax API
- 清理多余辅助方法，精简代码

---

## v1.0.0 (2026-07-15)

### 新增
- 首次发布
- 从 pixiv.re 等第三方图床下载 Pixiv 作品
- 单图直发 JPG，多图打包 PDF/ZIP 发送
- 支持三个图床源切换：pixiv.re / pixiv.cat / i.pixiv.re
- 可选本地保存图片/打包文件
- 简洁输出，不展示作品信息，直接发图
